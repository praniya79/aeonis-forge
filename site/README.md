# Site (Hugo) - placeholder

This folder is reserved for the blog site.

Next step: choose site name + whether to auto-push to GitHub.
Then we can scaffold Hugo (`hugo new site`) and wire publishing from `innovation-factory` into `site/content/posts/`.
